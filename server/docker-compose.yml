version: '3.8'

services:
  beathub-api:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always

# Note: MongoDB is assumed to be Atlas (Cloud-hosted) for production
